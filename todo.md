# TODO

- [ ] **Create a Dashboard**
  - Show a list of all configured apps
  - Display run count for each app (how many times it has been started)
  - Full timeline of start/stop events per app
  - Capture and display stdout/stderr for each run
  - Show sleep duration (time between stop and next start)
  - Show run duration (time from start to stop)

- [ ] **Support ACME / Let's Encrypt**
  - Add automatic TLS certificate provisioning via ACME protocol
  - Enable HTTPS support with auto-renewed certificates
